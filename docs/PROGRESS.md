# R3.5 数据优先重建进度

当前目标：一个 SQLite 数据库、一份可追溯历史数据、6 项基础指标。开发顺序固定为数据库与真实数据 → 指标算法及配置工厂 → 显示页面。5 个接口、28 个字段组件、44 个响应路径、6 项公式和 8 张表均为待用户核实的设计，不代表 CONTRACT_PASS。

| ID | 工作 | 状态 | 证据/剩余项 |
| --- | --- | --- | --- |
| RDOC-01 | 重写并归类最终 R3.5 文档 | 已完成编制，待用户核实 | 根目录 README 和 7 份设计/核实文档 |
| CLEAN-01 | 停止旧诊断服务并清理旧项目 | 已完成 | 旧容器、代码、页面、测试、配置、契约、数据库、备份、交付包及迭代脚本已删除 |
| D0 | 用户核实数据指标，开发者闭合接口可验证信息 | 待核实 | ../数据指标核实表R3.5.md；保留 5 源说明和样本 |
| D1 | 数据库与真实历史闭环 | 未开始 | 重新建立最小 Docker 环境；前 6 张数据表、真实入库/查询/回放/恢复 |
| D2 | 基础指标与配置工厂 | 未开始 | 再建 2 张结果表；输入只取 D1 数据 |
| D3 | 一个只读显示页 | 未开始 | D1/D2 验收后实施 |

2026-09-07，用户授权按建议清理。服务器旧 `stockreal-dashboard-1` 已停止并移除，`127.0.0.1:18080` 不再监听；未发现 StockReal 进程或项目定时任务。旧系统运行库及 WAL/SHM、备份、代码、页面、测试、配置、契约、同步包和根目录 21 个开发脚本已删除。

保留项：Git、AGENTS、根目录最终文档、服务器 `.secrets`、`apitoken`，以及 `data/` 中 DOC-008/009/034/035/036 各一份接口说明和真实样本。证据有 SHA256 清单，样本敏感字段扫描通过。没有创建新数据库、算法或页面，没有发送新接口请求；自动采集和方向输出保持关闭。

本地同仓库已同步清理；因自动审批拒绝递归删除，本地旧实现、旧版本归档和 `参考.png` 改为移出工作区，暂存在系统临时隔离目录 `stockreal-old-project-20260907`，可以恢复。只存在于本地的 `wff-v1.9.2-skills-install-pack/` 与 StockReal 无关，未纳入删除范围。

旧项目已经没有可运行应用，因此旧 128 项测试不再作为当前工作区测试入口。D1 开始时重新创建最小 Docker 开发环境，并按 AGENTS.md 在服务器 Docker 内执行开发、测试与部署。测试结果不能替代 D0 用户核实。
