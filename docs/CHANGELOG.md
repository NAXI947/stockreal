# 开发完成记录

## 2026-09-05 前序工作（依据既有证据补录）

- S0-01：Docker环境落地；compose.yaml固定镜像；容器内8项测试通过。
- S0-02：117条脱敏候选契约导入；凭据专用目录与Git排除；已知凭据扫描0命中。
- S0-03：11次手动请求均HTTP 200/JSON，DOC-075/081为空；报告stage0/first-probe.json。字段仍UNVERIFIED。
- 用户决定：暂无核对日历，自动采集保持关闭；不创建分支，不做二三期。

## 2026-09-05T10:58:38+08:00 GOV-01 进度控制建立

- 完成任务拆分、设计章节映射、依赖和逐项验收条件；基线文件保存五份R3.5文档SHA256。
- 证据：PROGRESS.md、design-baseline.json；AGENTS.md追加每项完成必须记录规则。
- 状态边界：计划已建立，不代表业务功能实现。
- 开始S0-04：业务响应/结构漂移/离线回放，本轮不新增真实接口请求。

## 2026-09-05T11:00:02.291467+08:00 S0-04 完成

- 代码：response_validation.py、replay.py、stage0.py集成。
- 验证：docker compose run --rm dev，23 tests OK；python -m stockreal.design_guard：PASS；python -m stockreal.replay：11/11完整性和结构匹配，其中2个EMPTY。
- 不增加真实接口请求；所有production_eligible=false，CONTRACT_PASS=0。
- 设计解释待最终字段冻结：样本哈希用于归档样本完整性，新市场数值不直接等同列结构漂移；结构版本单独校验。
- 开始S0-05：请求单股约束、日历和默认禁用门。

## 2026-09-05T11:03:43.789310+08:00 S0-05 完成

- 34项容器测试通过；design_guard PASS；runtime_gate确认CALENDAR_MISSING且自动采集/方向提示均false。
- 5个单股端点拒绝逗号、JSON、竖线、重复和数组StockID；未新增上游调用。
- 日历覆盖当前及下一年，检查缺失、过期、重复、闰日、人工核对哈希；没有生成或批准真实日历。
- P1-01拆为a/b：先做不依赖交易字段的SQLite诊断持久化；Signal/Outbox仍在后续，不将基础存储完成当作通知完成。

## 2026-09-05T11:12:03+08:00 P1-01a 完成

- 依据：开发方案§3/7；代码audit_store.py；数据库版本1，仅endpoint_contract/job_run/data_quality_event。
- 首次回滚恢复测试出现挂起：异常传递去除共享worker traceback后恢复；加入5秒超时回归和批次间协作让出执行权。
- docker compose run --rm dev：42 tests OK（7.887s）；包括100并发重复、25独立批次、整批回滚、重启和重复导入、版本拒绝。
- 两次独立容器执行python -m stockreal.audit_store：inserted=true后false；均117 contracts/1 run/11 events/WAL。
- 未生成信号、未发送通知；Signal/Outbox保留P1-01b。同步磁盘短时延迟保留P1-02性能验证。
- 更新进度依赖：P1-02离线开发只依赖P1-01a，生产运行仍受日历和字段契约阻断。

## 2026-09-05T15:35:56.522113+08:00 开始P1-02

- 两端哈希一致，design_guard PASS。
- 拆为a预算与I/O、b缓存重试、c任务计划、d真实接线验收，保持原范围。a~c只离线开发，d受日历及字段契约阻断。

## 2026-09-05T15:37:20.914863+08:00 P1-02a完成

- Docker内48 tests OK（7.139s）。SQLite版本2增加api_daily_budget，原审计保留。
- 单一专用线程负责连接、迁移、读写、回滚和关闭，模拟慢磁盘时主事件循环继续运行。
- 60000普通预算/20000预留/80000硬上限；20并发竞争最后1次额度仅放行1次，重启/跨日不重置旧日计数。
- 仅离线测试，实际生产数据库迁移将在本轮最终入库检查执行；无上游调用。开始P1-02b缓存与重试。

## 2026-09-05T15:45:30.286002+08:00 P1-02b完成

- Docker内66 tests OK（2.932s），gateway_core.py仅依赖注入的模拟传输，未安装HTTP适配器。
- 30并发同请求合并、取消单个消费者不取消共享请求、账号/契约/时段缓存隔离、TTL与源时间分别判断。
- host/endpoint并发2、端点软令牌桶、按请求最短间隔与随机抖动；错误降频和连续5成功恢复。
- 401/403停止；429遵守Retry-After；5xx/超时最多2次重试；重试逐次计预算；旧缓存标STALE且production_eligible=false。
- 开始P1-02c纯任务计划及交易时段回放；真实接线仍待P1-02d。

## 2026-09-05T15:49:40+08:00 P1-02c完成及本轮集成检查

- Docker内78 tests OK（3.384s），覆盖UTC转换、竞价/静默、午休、AM/PM独立窗口、60秒预热、14:56:30到期、最多20持仓、禁止候选L2与广度扇出。
- python -m stockreal.schedule_plan：CALENDAR_MISSING、tasks=[]、automatic_execution=false。
- python -m stockreal.audit_store：inserted=false，117 contracts/1 run/11 events，实际数据库完成v2迁移且无已记录网络请求。
- python -m stockreal.design_guard：PASS。S0-06/07/08和P1-02d仍需真实字段、广度、日历与交易日运行证据，不能以模拟测试代替。

## 2026-09-05T16:11:56.733622+08:00 S0-06补测、S0-07降级判定；开始P1-04a

- 依据接口清单§16.2/16.3及开发方案C-38/41，Docker内84项测试通过（5.163s）。
- `python -m stockreal.contract_evidence --manual`：7次有预算记账请求；075 Time=0返回242行，081使用已存009行情日期返回30行；041分页270个唯一代码，与Count一致。
- 新增样本出现SCHEMA_DRIFT，保留原冻结观察基线，不自动晋升；时间/单位/交易时段待验证。
- 未确认批量N_up/N_valid和成分股净流入，Breadth/SectorHeatScore/DivergenceBaseScore/DivergenceScore均禁用；完整行数不证明原子行情截面。S0-07按设计的明确降级出口完成，S0-06仍待外部验收。
- 自动采集及方向提示继续关闭。继续P1-04a手工持仓存储，原P1-04整体验收不变。

## 2026-09-05T16:15:41.356278+08:00 P1-04a完成；开始P1-07a

- 持仓代码、参考成本、观察级别由单写入队列持久化；最多20条、可留空成本、版本冲突拒绝、历史修订保留。无虚构持仓或上游请求。
- 修正科学计数法与分隔符误接受，Docker内90项测试通过；v2迁移预算保留、重启、并发编辑、无效输入回滚均覆盖。
- 依据开发方案§8.1/8.3，开始六页只读诊断预览；只呈现真实配置与明确空态，不启用行情业务。

## 2026-09-05T16:22:32.603763+08:00 P1-07a完成；P1-07b持仓编辑接线

- 91项Docker测试通过（4.501s），DESIGN_BASELINE_PASS。六页浏览器导航与真实0/117契约、7次手动预算、270板块覆盖及未评估空态已检查。
- dashboard容器部署，仅绑定127.0.0.1:18080，无.secrets挂载；HTTP私有路径404、恶意Host403、POST405、健康200。
- SSH本机隧道预览可用。开始手工持仓页面编辑；P1-08仍需备份恢复证据及最终业务/外网验收。

## 2026-09-05T16:34:57.497659+08:00 P1-07b、P1-08a/b完成

- 依据开发方案§8.1(2)/8.3/7.4/11，持仓页面可增删草稿并保存；观察级别仅个人标签，不改变采集频率。PUT需同源Origin、固定Host和专用头；大小/类型/字段校验及版本冲突拒绝。
- Docker内95项测试通过（7.091s）；包含隔离HTTP请求、拒绝跨站、无效数据不写入、快照不覆盖、哈希篡改拒绝与恢复副本对照。
- 浏览器完成六页导航、输入错误拦截、空配置幂等保存（版本0）和390px窄屏检查。实际账户没有示例持仓。
- 实际备份stockreal-20260905T162933534691.db，SHA256=2ffbf1e4306b7acf8916306b7854d9326a962cce793a98c6466c9edc507deb53。隔离恢复保留117契约/11事件/预算7，未覆盖运行库。
- dashboard只映射服务器127.0.0.1:18080，本机SSH隧道可访问；Docker健康检查及重启通过。应用仅挂载必要代码、配置和数据，不挂载.secrets。
- P1-08全项仍待真实业务集成；阶段0语义/日历和五日/十日外部验收没有被勾为完成。

## 2026-09-05T17:24:36.188707+08:00 S0-06a完成：定位结构差异与字段证据

- 设计依据：接口清单§16.2/16.3；实际开发、测试和报告生成均在服务器Docker内。
- 从所有者提供的catalog.sample_response_raw提取4个接口候选位置映射并记录来源SHA256；未知/重复列明确停用，金额/数量单位仍UNVERIFIED。
- schema_diff输出路径、类型和列数差异，不输出响应值；空数组基线标记ITEM_SHAPE_NOT_OBSERVED。回放前校验原样本与补测样本哈希和路径边界；报告超长时显式truncated。
- DOC-041 Index=180差异定位：第7/8行第4列、第37/56行第11列由基线float变为本次int（均从0计数）。不自动扩大已存基线类型。
- DOC-013 32/32时间文本与Unix秒解码一致，32/32金额/(量×均价)接近100；041 270/270、081 30/30资金列带符号加总相符。仅记录一致性，未据此猜测单位或生成方向提示。
- 修复request_url接受未解析命名占位符的问题，Time={time_param}在联网前被拒绝；受限手动工具的Time=0不受影响。
- 命令：docker compose run --rm dev python -m unittest discover -s tests -q，107项通过（10.298s）；python -m stockreal.contract_review离线生成7样本审阅报告；python -m stockreal.design_guard通过。
- 本轮新增行情请求0；S0-06全项仍等待单位、时间语义及交易时段证据，S0-08继续按用户确认保持禁用。
